// while loops

let tries = 0;
while (tries < 3) {
    console.log("Try number: " + (tries + 1));
    tries++;
}

let count = 10;
while (count >=1) {
    console.log("Countdown: " + count);
    count--;
}